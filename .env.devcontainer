# Database configuration for devcontainer environment
DB_HOST=db
DB_NAME=anorm_test
DB_USER=dev
DB_PASS=dev
DB_PORT=3306

# Alternative variable names for compatibility
DB_DATABASE=anorm_test
DB_USERNAME=dev
DB_PASSWORD=dev

# MariaDB root password
DB_ROOT_PASSWORD=root

# PHP configuration
PHP_VERSION=8.1
